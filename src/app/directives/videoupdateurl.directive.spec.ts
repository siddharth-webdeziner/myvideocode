import { VideoupdateurlDirective } from './videoupdateurl.directive';

describe('VideoupdateurlDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoupdateurlDirective();
    expect(directive).toBeTruthy();
  });
});
