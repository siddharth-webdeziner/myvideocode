import { NewvideosDirective } from './newvideos.directive';

describe('NewvideosDirective', () => {
  it('should create an instance', () => {
    const directive = new NewvideosDirective();
    expect(directive).toBeTruthy();
  });
});
